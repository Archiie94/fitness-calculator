import { AnimateInDirective } from './animate-in.directive';

describe('AnimateInDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateInDirective();
    expect(directive).toBeTruthy();
  });
});
